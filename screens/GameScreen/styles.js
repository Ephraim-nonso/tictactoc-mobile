import { StyleSheet } from "react-native";
import Color from "../../constants/Color";

export const styles = StyleSheet.create({
  gameName: {
    marginBottom: 30,
    textDecorationColor: Color.secondaryColor,
    textDecorationLine: "underline",
    textDecorationStyle: "double",
  },
});
